function callback(){
    const minhaFunc = (num, num2, peracao) => {
        return operacao(um1, num2);
    }
    //operacao = (a, b ) => (a +  b)
    const calcular = (a, b) => {return "Vai Corinthiansa!" + b}

    
    
    minhaFunc();
}